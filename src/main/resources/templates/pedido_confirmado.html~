<!DOCTYPE html>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
  <meta charset="UTF-8">
  <title>Pedido Confirmado - Michelladas</title>
  <link rel="stylesheet" th:href="@{/css/confir.css}">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<div class="container my-5">
  <!-- Logo -->
  <div class="text-center mb-4">
    <img th:src="@{/imagenes/Logo.svg}" alt="Logo Michelladas" height="80">
  </div>

  <!-- Mensaje principal -->
  <div class="card shadow-sm p-4 mx-auto" style="max-width: 600px;">
    <h2 class="text-center text-success mb-3">¡Pedido confirmado con éxito!</h2>
    <p class="text-center mb-4">Gracias por tu compra. Tu pedido ha sido recibido y está siendo preparado.</p>

    <!-- Resumen breve -->
    <div class="mb-2"><strong>ID del pedido:</strong> <span th:text="${pedidoConfirmado.id}">#123</span></div>
    <div class="mb-2"><strong>Fecha:</strong> <span th:text="${#temporals.format(pedidoConfirmado.fecha, 'dd/MM/yyyy HH:mm')}">00/00/0000</span></div>
    <div class="mb-2"><strong>Total:</strong> <span th:text="${#numbers.formatDecimal(pedidoConfirmado.total, 1, 'POINT', 2, 'COMMA')}">$0.00</span></div>
    <div class="mb-2"><strong>Método de pago:</strong> <span th:text="${pedidoConfirmado.metodoPago}">Efectivo</span></div>
    <div class="mb-2"><strong>Estado:</strong> <span th:text="${pedidoConfirmado.estado}">PENDIENTE</span></div>

    <!-- Botones -->
    <div class="text-center mt-4">
      <a th:href="@{/menu}" class="btn btn-outline-success me-2">Volver</a>
      <a th:href="@{/pedido/historial}" class="btn btn-outline-custom">Mis pedidos</a>
      <button class="btn btn-outline-success ms-2" data-bs-toggle="modal" data-bs-target="#whatsappModal">¿Dudas? WhatsApp</button>
    </div>
  </div>
</div>

<!-- Modal WhatsApp -->
<div class="modal fade" id="whatsappModal" tabindex="-1" aria-labelledby="whatsappModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content shadow-sm">
      <div class="modal-header">
        <h5 class="modal-title text-modal" id="whatsappModalLabel">¡Contáctanos por WhatsApp!</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
      </div>
      <div class="modal-body text-center">
        <p>Si tienes alguna duda o quieres hacer un cambio, escríbenos por WhatsApp y te atenderemos.</p>
        <a th:href="'https://wa.me/573214139231?text=Hola, acabo de hacer un pedido con ID #' + ${pedidoConfirmado.id}"
           class="btn btn-outline-custom" target="_blank">
          <i class="bi bi-whatsapp"></i> Ir a WhatsApp
        </a>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
